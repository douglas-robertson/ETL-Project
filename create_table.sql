
-- Create Two Tables
CREATE TABLE country_height (
	country VARCHAR,
	male_height_cm float(3),
	female_height_cm float(3),
	average_height_cm float(3)
);

CREATE TABLE olympics_country (
	country VARCHAR,
	gold_count INT,
	silver_count INT,
	bronze_count INT,
	total_count INT
);

